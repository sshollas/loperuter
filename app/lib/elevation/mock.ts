export function estimateGainLoss(nodeIds: number[]): { gain: number; loss: number } {
  // Placeholder – integration point for DEM sampling (Kartverket / Mapbox Terrain).
  // For now we return zero so the API stays deterministic.
  return { gain: 0, loss: 0 };
}
